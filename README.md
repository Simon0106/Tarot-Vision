# Tarot-Vision

Contextual tarot reading app with AI-powered interpretations.

## Setup

1. Copy `.env.local.example` to `.env.local`
2. Fill in your API keys
3. Run `npm install`
4. Run `npm run dev`
